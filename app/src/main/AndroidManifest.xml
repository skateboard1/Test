<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.skateboard.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity
            android:name=".LayoutAnimatorActivity"
            android:theme="@style/OtherTheme" />
        <activity android:name=".ZoomActivity" />
        <activity android:name=".ConnectActivity" />
        <activity
            android:name=".TicketActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".InputLayoutActivity" />
        <activity android:name=".MyViewPagerActivity" />
        <activity android:name=".ConsumeViewActivity" />
        <activity android:name=".CanvasActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".TotalCounsumeActivity" />
        <activity android:name=".FinalIndicatorShowActivity"></activity>
    </application>

</manifest>
